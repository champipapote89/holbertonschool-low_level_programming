#include <stdio.h>
#include <stdlib.h>
/**
 *main - Entry point
 *prints exactly with proper grammar, but the outcome is a piece of art,
 *Return: Always 0 (Success)
 */
int main(void)
{

int putchar(char, int, long int, long long int, float);
printf("Size of a char: %c bytes(s)", char);
printf("Size of a int: %d bytes(s)", int);
printf("Size of a long int: %ld bytes(s)", long int);
printf("Size of a char: %lld bytes(s)", long long int);
return (0);
}
